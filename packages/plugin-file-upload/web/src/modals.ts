import FileUploadSettingsModal from './toolbar/file-upload-settings';

const Modals = {
  FILE_UPLOAD_SETTINGS: 'file-upload-settings',
};

const ModalsMap = {
  [Modals.FILE_UPLOAD_SETTINGS]: FileUploadSettingsModal,
};

export { Modals, ModalsMap };
